name: sample only
on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      ["main"]
  workflow_dispatch:

  jobs:
    first-job:
      on:
        pull_request:
          path:
            "./scr/main/java/service/DemoServiceImpl.java"
      steps:
        - name: first-job first STEP
          run: echo "First job first step"
    second-job:
      - name: NO job
        run: echo "NO JOB HERE"

